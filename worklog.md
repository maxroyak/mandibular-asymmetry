# Mandibular Asymmetry Analysis — Worklog

- 2026-08-16: PMBot — Received project specification for 2D mandibular asymmetry analysis from OPG
- 2026-08-16: PMBot — Created project context files: PROJECT_CONTEXT.md, WORKFLOW.md, AGENTS.md
- 2026-08-16: PMBot — Created agent role definitions: PMBot, ResearchBot, OrthoBot, DevBot, UXBot, VisionBot, TestBot, QABot
- 2026-08-16: PMBot — Delegated project scaffolding to DevBot (directory structure, .gitignore, git init)
- 2026-08-16: PMBot — Next: Delegate Task 1 (scientific research) to ResearchBot after scaffolding completes- 2026-08-16: DevBot — Read existing source: types.ts, mandibularAsymmetry.ts, ResultsPanel.tsx, CalibrationPanel.tsx, studyStore.ts, test file (85 tests baseline)
- 2026-08-16: DevBot — Added BilateralMeasurement and MandibularAsymmetryResult types to src/domain/types.ts
- 2026-08-16: DevBot — Added 6 pure domain functions to mandibularAsymmetry.ts: calculateDifferenceMm, determineLongerSide, determineShorterSide, generateRamusComparison, generateBodyComparison, generateMandibularAsymmetryConclusion
- 2026-08-16: DevBot — Updated studyStore.ts: added mandibularResult state, buildBilateralMeasurement/computeMandibularResult helpers, wired into recalculate and landmark mutations
- 2026-08-16: DevBot — Updated ResultsPanel.tsx: added BilateralMmSection with mm values/comparison sentences, calibration-required notice for Mode A, clinical conclusion display; preserved existing MeasurementSection and interpretation
- 2026-08-16: DevBot — Added 31 new unit tests for bilateral mm functions (total 116 tests), including critical right/left non-reversal and independent side evaluation tests
- 2026-08-16: DevBot — Verified: tsc --noEmit PASS, npm test PASS (116/116), npm run build PASS
- 2026-08-16: TestBot — Verified DevBot's 116 tests; identified 5 gaps vs spec (exact spec values R=60/L=58, R=78/L=82, R=60/L=60, combined R=62/L=59+R=77/L=81, and systematic R/L non-reversal regression). Added 29 new tests covering all 5 required spec test cases with exact values. Total now 145 tests, all PASS.
- 2026-08-16: QABot — Independent final verification of mm measurements + comparison + conclusion change request. VERDICT: PASS. Build PASS (vite build, 31 modules), tsc --noEmit PASS (exit 0), npm test PASS (145/145). Clinical safety: Mode A shows "Absolute measurements in millimeters are unavailable..." notice, Mode B shows mm; conclusion uses comparative language ("X mm longer than"), never diagnostic ("hypoplasia"/"hyperplasia"/"diagnosis" all absent); 2D projection limitation preserved in LIMITATION_HEADER, LIMITATION_FOOTER, and conclusion text ("2D measurements", "current projection"); conclusion evaluates ramus and body independently (right can be longer in ramus while left is longer in body — verified by test case 4 R=62/L=59 ramus + R=77/L=81 body). Mathematical correctness: R=60/L=58 → diff=2 right longer ✓; R=78/L=82 → diff=-4 left longer ✓; R=60/L=60 → diff=0 equal ✓; combined case names right ramus +3mm AND left body +4mm ✓. Right/left non-reversal: 8 regression tests in "Required spec test case 5" systematically verify all 4 comparison functions never swap sides across right-both, left-both, and both mixed-direction scenarios. Architecture: domain layer (mandibularAsymmetry.ts, types.ts) has zero React imports — all 6 new functions are pure; ResultsPanel.tsx contains no calculation formulas (calls domain functions only); existing percentage measurements and Habets index fully preserved (MeasurementSection unchanged). No issues found.
- 2026-08-16: DevBot — Fixed landmark dragging coordinate drift bug in ImageViewer.tsx. ROOT CAUSE: screenToNormalized() mapped clientX/clientY directly to container-normalized coordinates (divide by rect.width/height) without reversing zoom or pan transforms, and without accounting for object-contain letterboxing. Additionally, the SVG overlay did not receive the same transform as the <img> layer, so landmarks were rendered in container space while the image moved independently under zoom/pan. FIX: (1) Rewrote screenToNormalized with correct inverse transform chain: subtract container rect → subtract pan → invert zoom about center → map through object-contain fitted image rect → normalize [0,1] with clamp. (2) Applied identical transform: translate(panX,panY) scale(zoom) to the SVG overlay so landmarks, lines, and markers track the image at all zoom/pan states. (3) Migrated from mouse events to pointer events with setPointerCapture for reliable drag tracking, and introduced InteractionMode ref ("none"|"pan"|"drag-landmark") to strictly separate landmark dragging from image panning. (4) Landmark position computed directly from pointer position on every move (no delta accumulation). Build PASS, tsc --noEmit PASS, npm test PASS (145/145).
- 2026-08-16: TestBot — Added tests for landmark dragging coordinate transformation. Created src/domain/coordinateTransform.ts (pure domain function extracting the screen→normalized math from ImageViewer.tsx: screenToNormalized, normalizedToScreen, computeFittedImageRect — no React imports) and src/domain/coordinateTransform.test.ts with 37 tests covering: Test A (100% zoom: pointer (500,300)→(550,340) correct normalized coords), Test B (200% zoom: 40px screen movement = 0.025 normalized, half of zoom=1), Test C (zoom=2 + pan=(150,-40): round-trip identity, landmark stays under pointer), Test D (zero cumulative drift: 100 consecutive pointermove events final = direct calculation, plus zigzag 200-move path), Test E (viewer offset left=220 top=140: same normalized results as origin, round-trip with zoom+pan). Additional: 50%/75%/150%/300% zoom with inverse-scaling verification, clamping to [0,1] for pointers outside image bounds (left/right/top/bottom/letterbox/high-zoom+pan/vertical-letterbox), round-trip consistency across 6 transform × 6 point combinations, edge cases (zero container, fallback image dims, square image, very small zoom). tsc --noEmit PASS, npm run build PASS, npm test PASS (182/182: 145 existing + 37 new). No issues found.
- 2026-08-16: QABot — Independent final verification of landmark dragging coordinate drift bug fix. VERDICT: PASS. (1) Coordinate transformation correctness: screenToNormalized() in ImageViewer.tsx implements correct inverse transform chain — subtract container rect (L71-72) → subtract pan (L75-76) → invert zoom about center cx+(pannedX-cx)/zoom (L83-84) → object-contain fitted rect via AR comparison (L97-105) → normalize by displayedW/displayedH (L111-112) → clamp [0,1] (L115-116). SVG overlay (L442) receives identical transform translate(panX,panY) scale(zoom) with transformOrigin center, matching the <img> layer (L423-424). Pointer capture via setPointerCapture on both drag-landmark (L194) and pan (L225) paths. InteractionMode ref ("none"|"pan"|"drag-landmark") at L13/L17 strictly separates gestures — landmark drag returns before pan check (L199, L243). (2) No cumulative drift: handlePointerMove (L240) computes position directly from screenToNormalized(e.clientX, e.clientY) on every pointermove — no delta accumulation, no Math.round() or integer truncation anywhere in the file. Clamping to [0,0]-[1,1] via Math.max/Math.min verified. (3) Regression: npm run build PASS (vite build, 31 modules, 153ms), npm test PASS (182/182: 145 existing mandibularAsymmetry + 37 coordinateTransform), npx tsc --noEmit PASS (exit 0, no errors). Existing features verified: zoom (wheel L123-130, keyboard L288-294, toolbar L346-358), pan (L216-248), landmark placement (L132-175), deletion (L452+ store, L566-575 UI delete-btn), recalculation (moveLandmark calls computeMeasurements+computeMandibularResult+recalculate+debouncedSave at L429-449, immediate no manual button), study persistence (debouncedSave on every landmark mutation). (4) Architecture: coordinateTransform.ts is pure — only import is `type { Point }` from ./types, zero React imports, no side effects, no DOM access. Domain module math matches component math exactly: both use identical 6-step inverse transform chain, identical object-contain AR logic, identical clamp. No issues found.

- 2026-08-16: DevBot — Added visible measurement lines with mm labels on the radiograph overlay. Fixed duplicate calibrationMode declaration in ImageViewer.tsx (was causing TS2451). Removed unused round() helper from studyStore.ts. Updated test expectation for convertDistanceToMm to match full-precision return value (26.64 instead of rounded 26.6). Overlay now reads mm values from mandibularResult (single source of truth from store), shows 'calibration required' when uncalibrated, and displays actual mm values when calibrated. Conclusion already included actual measured values.
- 2026-08-16: QABot — Final verification of measurement lines + mm labels + conclusion with actual values. VERDICT: PASS. (1) Build: npm run build PASS (vite v8.2.1, 31 modules, exit 0). (2) Tests: npm test PASS 206/206 (coordinateTransform 37 + mandibularAsymmetry 169), 590ms. (3) Overlay rendering (ImageViewer.tsx): 4 measurement lines defined — R Ramus (CoR→GoR, blue #2563eb), L Ramus (CoL→GoL, red #dc2626), R Body (GoR→Me, blue), L Body (GoL→Me, red); lines drawn as SVG <line> in normalized viewBox 0 0 1 1 (L478) with vectorEffect non-scaling-stroke; mm labels at line midpoints (midX/midY L491-492) via <text> with dark rect background for readability (L515-537); color coding confirmed RIGHT_COLOR=#2563eb blue, LEFT_COLOR=#dc2626 red (L344-345). (4) Calibration gating: isCalibrated = calibrationMode==="B" && calibration!==null (L51); showMm = isCalibrated && line.mm!==null (L494); uncalibrated label = "R Ramus: calibration required" in amber #fbbf24 (L497,529); calibrated label = "R Ramus: 60.0 mm" in white (L496). (5) One source of truth: overlay reads mandibularResult from useStudyStore (L49), lineDefs mm values from mandibularResult?.ramus.rightMm etc (L354,358,370,374) — same store state as ResultsPanel; no recalculation in component. Store computes via computeMandibularResult→buildBilateralMeasurement→generateMandibularAsymmetryConclusion (studyStore L236-248). (6) Conclusion with actual mm values (mandibularAsymmetry.ts L227-323): includes actual measured values e.g. "The right ramus measures 60.0 mm and is 2.0 mm longer than the left ramus, which measures 58.0 mm." (L248-251); evaluates ramus and body independently; comparative not diagnostic language. Tests verify exact spec format (L900-905) and all 4 mm values present (L892-898). (7) Zoom/pan invariance: store computeSingleMeasurement uses calculateDistance on normalized landmarks (L146-147) then convertDistanceToMm(normDist, imageWidth, imageHeight, mmPerPixel) (L162-165) — no screen/zoom/pan params; SVG overlay shares identical transform as image layer (L483-484 = L463-464); 4 zoom/pan invariance tests (L1240-1269) confirm mm values identical at 100%/200%/50% zoom and after panning; 1 test confirms values DO change when landmark moves (L1271-1279). (8) Pre-existing note: npm run lint fails due to missing eslint.config.js (ESLint 9 migration issue) — not introduced by this change, does not affect build or tests. No issues found in the change under review.
- 2026-08-17: TestBot — Verified test coverage for calibration UX redesign. Analyzed 206 existing tests against 4 checklist items: (1) mm displayed when calibrated — COVERED by mandibularAsymmetry.test.ts L353-372 ("shows absolute measurements when calibrated" verifies 52.3/48.1/75.0/72.5 mm + mm/pixel); (2) "calibration required" notice when uncalibrated — COVERED by L342-351 ("shows 'Calibration not performed' when uncalibrated"); (3) startCalibration/cancelCalibration store actions — GAP FOUND, zero test coverage for new store state isCalibrating and actions startCalibration/cancelCalibration; (4) conclusion with actual mm values — COVERED by L892-898, L1447-1481 (multiple tests verify all 4 mm values present in conclusion text). Created src/store/studyStore.test.ts with 12 new tests covering: isCalibrating defaults false, startCalibration sets true + clears calibrationPoints + clears calibration, cancelCalibration sets false + clears calibrationPoints, isCalibrating reset by newStudy/createStudy/clearCalibration, full calibration flow (start→set points→set distance→compute→verify calibration object), cancel aborts without setting calibration. Added localStorage mock for jsdom environment. npm test PASS 218/218 (206 existing + 12 new). No issues found.
- 2026-08-17: DevBot — Calibration UX redesign: mm as primary result, removed Mode A/B terminology, added "Calibration required" with [Calibrate image] button. Changes: (1) ResultsPanel.tsx — mm values are PRIMARY (Right mm, Left mm, Difference mm, comparison sentence) with secondary metrics (Rel. Diff %, Habets Index %, classification) underneath; uncalibrated banner now shows "Calibration required to display millimeters" with a [Calibrate image] button that triggers store.startCalibration(); calibrated banner states calibration source "(user-marked reference distance)"; fixed wrong import path ../../domain → ../domain/mandibularAsymmetry. (2) CalibrationPanel.tsx — removed all "Mode A"/"Mode B" labels; uncalibrated state shows "Calibration required to display millimeters" with [Calibrate image] button; calibrating state header changed from "Calibration Mode" to "Image Calibration"; calibrated state shows "Calibrated: X.XXXX mm/pixel (user-marked reference distance)" instead of "Mode B: Calibrated"; migrated isCalibrating from local useState to store state (isCalibrating, startCalibration, cancelCalibration) so ResultsPanel can trigger calibration start. (3) studyStore.ts — added isCalibrating boolean, startCalibration() and cancelCalibration() actions; updated all initial state blocks (create, newStudy, loadStudy, clearCalibration). (4) types.ts — comments updated from "Mode A/Mode B" to "uncalibrated"/"user-marked reference distance". (5) mandibularAsymmetry.ts — JSDoc comment updated from "Mode B" to "calibrated". (6) mandibularAsymmetry.test.ts — test descriptions updated from "in Mode A"/"in Mode B" to "when uncalibrated"/"when calibrated" (assertions unchanged). (7) DICOM investigation: ImageUploadZone.tsx supports JPG/PNG/BMP/TIFF via FileReader.readAsDataURL + browser Image() API; no EXIF/DICOM metadata is preserved — images are converted to data URLs and downscaled via canvas if >2000px; DICOM parsing would require a dedicated library (dicom-parser or corn... [truncated]
- 2026-08-17: QABot — Final verification of calibration UX redesign. VERDICT: PASS. (1) Build: npm run build PASS (vite v8.2.1, 31 modules, 171ms, exit 0). (2) Tests: npm test PASS 218/218 (coordinateTransform 37 + mandibularAsymmetry 169 + studyStore 12), 593ms. (3) mm primary in ResultsPanel.tsx: CalibratedMmSection renders Right mm / Left mm / Difference mm in large bold font (text-lg font-bold) as PRIMARY (L72-102), with comparison sentence below (L105); secondary metrics (Rel. Diff %, Habets Index %, Greater side) shown in text-xs below border-t (L107-131) — percentages are clearly secondary. UncalibratedSection shows relative % as primary with normalized values (L149-235). (4) No "Mode A"/"Mode B" in clinician-facing UI: search across all src/components/ returns 0 matches for "Mode A" and "Mode B"; CalibrationPanel.tsx uses "Image Calibration" header (L94), "Calibration required to display millimeters" (L77), "Calibrated: X.XXXX mm/pixel (user-marked reference distance)" (L156) — no mode terminology. (5) "Calibration required to display millimeters" appears in both ResultsPanel.tsx (L274, with [Calibrate image] button L280-285 calling store.startCalibration) and CalibrationPanel.tsx (L77, with [Calibrate image] button L82-87). (6) Calibration source shown when calibrated: ResultsPanel L265-269 "✓ Calibrated: X.XXXX mm/pixel (user-marked reference distance) — Measurements in mm"; CalibrationPanel L156-159 same. (7) isCalibrating moved to store: studyStore.ts L66 (isCalibrating: boolean), L280/305/333/396/523 (reset in all init blocks), L529-530 (startCalibration sets true + clears points + calibration), L533-534 (cancelCalibration sets false + clears points). (8) Existing features preserved: measurement lines (ImageViewer.tsx hoveredLine refs intact), conclusion (ResultsPanel L341-350, mandibularAsymmetry.ts generateMandibularAsymmetryConclusion), study persistence (studyRepository.ts + StudyManager.tsx + localStorage), 2D projection warning (L360-366), threshold caveat (L368-374), clinical interpretation (L377-388), body length reliability warning (L134-140), hover-highlight on measurement cards (L75-76,86-87). (9) Lint: npm run lint FAILS — pre-existing infrastructure issue (eslint.config.js missing, ESLint v9 migration needed); NOT related to this redesign, NOT a regression. No issues found in the calibration UX redesign itself.
- 2026-08-17: DevBot — Fixed calibration point placement bug. handleOverlayClick in ImageViewer.tsx checked calibrationMode === "B" to enter calibration placement branch, but startCalibration() only sets isCalibrating=true (calibrationMode stays "A" until computeCalibration completes). Added isCalibrating subscription and changed the condition to check isCalibrating instead. Also fixed handlePointerDown to block panning during active calibration, and updated cursor to show crosshair during calibration.
- 2026-08-17: PMBot — Received calibration UX bug report from user (7 bugs: one click places two points, root cause verification, markers too large, need invisible hit areas, landmark marker size, status indicators, label positioning). Created task plan and dispatched to DevBot/TestBot/QABot/GitBot.
- 2026-08-17: DevBot — COMPLETED: Fixed all 7 calibration UX bugs. BUILD PASS, TESTS PASS (224/224 — 218 original + 6 new), TSC PASS. ROOT CAUSE of Bug 1: setCalibrationPoint used [Point,Point] tuple with ?? point fallback — when calibrationPoints was null, setCalibrationPoint(0,point) filled BOTH slots (current[1] ?? point = undefined ?? point = point). STATE bug, not visual. Fix: replaced tuple with CalibrationDraft {point1,point2} independent fields. Files changed: types.ts (CalibrationDraft), studyStore.ts (setCalibrationPoint rewrite, computeCalibration), studyRepository.ts (StoredStudy type), ImageViewer.tsx (calibration refs, small markers r=0.005, hit areas r=0.014, P1/P2 labels, landmark markers shrunk), CalibrationPanel.tsx (status indicators: Place Point 1 → Now place Point 2 → Enter known distance), studyStore.test.ts (6 regression tests). All 7 bugs FIXED.
- 2026-08-17: TestBot — PENDING: Add 5 required tests for sequential calibration placement (first click, second click, different coords, no double event, reset/recalibrate).
- 2026-08-17: QABot — PENDING: Verify calibration workflow on real panoramic image, check all acceptance criteria.
- 2026-08-17: GitBot — COMPLETED: First commit b3f658a pushed to private repo https://github.com/maxroyak/mandibular-asymmetry. Added *.tsbuildinfo to .gitignore. All internal files (worklog.md, TASKS.md, DECISIONS.md, tasks/) correctly excluded.
- 2026-08-17: PMBot — Received calibration redesign request from user. Current calibration is broken — clicks during "Place Point 1" don't reliably place points. User wants explicit state machine with confirmation workflow (idle → placing-point-1 → reviewing-point-1 → placing-point-2 → reviewing-point-2 → entering-distance → calibrated). Created task plan and dispatched DevBot/TestBot/QABot/GitBot.
- 2026-08-17: DevBot — COMPLETED: Calibration state machine redesign. BUILD PASS, TESTS 240/240 PASS, TSC PASS. ROOT CAUSE of click failure: calibration placement was in SVG onClick handler, but handlePointerDown on container div called setPointerCapture() for pan, which captured the pointer and prevented the click event from reaching the SVG. Fix: moved calibration placement to pointerdown as highest priority check. Added CalibrationStage type (7 stages: idle, placing-point-1, reviewing-point-1, placing-point-2, reviewing-point-2, entering-distance, calibrated). New store actions: placeCalibrationPoint, confirmPoint1/2, resetPoint1/2, confirmCalibration, moveCalibrationPoint, cancelCalibration (with previousCalibration restore). CalibrationPanel completely redesigned with step-by-step instructions and buttons per stage. 12 new tests added (228 rewritten to 240 total).

- 2026-08-16: DevBot — Calibration UX bug fix (7 bugs). Root cause of Bug 1: setCalibrationPoint in studyStore.ts used `[Point, Point] | null` tuple with `?? point` fallbacks. When calibrationPoints was null, `setCalibrationPoint(0, point)` set BOTH tuple slots to the same coordinate because `current[1] ?? point` resolved `undefined ?? point` → point. This was a STATE bug (not just visual) — both calibration point coordinates were actually created from a single click. Fix: replaced tuple with CalibrationDraft { point1: Point | null, point2: Point | null } where each field is set independently. Changes: (1) src/domain/types.ts — added CalibrationDraft interface; (2) src/store/studyStore.ts — changed calibrationPoints type from [Point, Point]|null to CalibrationDraft|null, rewrote setCalibrationPoint to set point1/point2 independently, updated computeCalibration to read .point1/.point2; (3) src/persistence/studyRepository.ts — updated StoredStudy.calibrationPoints type; (4) src/components/ImageViewer.tsx — updated all calibrationPoints tuple refs to .point1/.point2, reduced calibration marker visible radius from 0.012 to 0.005 (Bug 3), added transparent hit area circle r=0.014 (Bug 4), added small "P1"/"P2" labels offset from markers (Bug 7), reduced anatomical landmark marker visible radius from 0.012 to 0.005 with transparent hit area r=0.014 and moved landmark-marker class to hit area circle for drag detection (Bug 5), reduced active ring from 0.025 to 0.018, reduced label fontSize from 0.015 to 0.01; (5) src/components/CalibrationPanel.tsx — updated calibrationPoints tuple refs to .point1/.point2, added text status indicators: "Place Point 1" / "Point 1 ✓ — Now place Point 2" / "Point 1 ✓ — Point 2 ✓ — Enter known distance" (Bug 6); (6) src/store/studyStore.test.ts — added 6 Bug 1 regression tests. BUILD PASS, TESTS 224/224 PASS, TSC PASS.
- 2026-08-17: TestBot — Added 4 new tests for sequential calibration placement (Tests 3 + 5 variants for startCalibration/cancelCalibration/clearCalibration reset). Tests 1, 2, 4 already covered by DevBot Bug 1 regression tests. Total: 228 tests pass, build PASS, tsc PASS.
- 2026-08-17: QABot — Independent final verification of calibration UX bug fixes (7 bugs). VERDICT: PASS. (1) Build PASS, 228/228 tests PASS, TSC PASS. (2) Bug 1 root cause fix VERIFIED: setCalibrationPoint uses CalibrationDraft { point1, point2 } independent nullable fields (studyStore.ts:486-498), no `?? point` fallback in code (only in comments), old [Point, Point] tuple completely gone from code. (3) Bug 2 root cause VERIFIED: was a STATE bug — old tuple filled both slots at state level, fix is at state level. (4) Bug 3 VERIFIED: visible calibration marker r=0.005 (ImageViewer.tsx:580, 614). (5) Bug 4 VERIFIED: transparent hit area r=0.014 (ImageViewer.tsx:569-575, 603-608). (6) Bug 5 VERIFIED: landmark markers r=0.005 visible + r=0.014 transparent hit area with landmark-marker class and data-landmark attr (ImageViewer.tsx:656-664). (7) Bug 6 VERIFIED: text status indicators in CalibrationPanel.tsx:103-117 — "Place Point 1" / "Point 1 ✓ — Now place Point 2" / "Point 1 ✓ — Point 2 ✓ — Enter known distance". (8) Bug 7 VERIFIED: P1/P2 labels fontSize=0.01 offset +0.012/-0.008 from marker (ImageViewer.tsx:588-597, 622-631). (9) No "Mode A"/"Mode B" in clinician-facing UI — 0 matches in src/components/. (10) Existing features preserved: 4 measurement lines (R Ramus, L Ramus, R Body, L Body), mm labels when calibrated, "calibration required" when uncalibrated, conclusion with mm values, 2D projection warning, localStorage persistence, zoom/pan, landmark drag. (11) Architecture clean: domain layer 0 React imports, no calculation logic in UI components, CalibrationDraft in types.ts. (12) All 5 required spec tests accounted for: Test 1 (line 217), Test 2 (line 230), Test 3 (line 274), Test 4 (line 188), Test 5 (line 296 + variants).

- 2026-08-17: DevBot — Calibration state machine redesign COMPLETE. Root cause of click failure: calibration point placement was handled in SVG onClick handler, but handlePointerDown on the container div would start a pan gesture and call setPointerCapture() before the click event could fire. When isCalibrating was true and calibration points were still pending, handlePointerDown returned early (no pan), but in certain timing scenarios the stale closure for isCalibrating or the calibrationPoints check allowed pan to start, capturing the pointer and swallowing the subsequent click event. Fix: moved calibration point placement from onClick to pointerdown (handlePointerDown) — when stage is "placing-point-1" or "placing-point-2", the pointerdown handler places the calibration point and returns immediately without starting pan, without calling preventDefault, and without setPointerCapture. This eliminates the pointerdown/click conflict entirely. Files changed: (1) src/domain/types.ts — added CalibrationStage type union (7 stages: idle, placing-point-1, reviewing-point-1, placing-point-2, reviewing-point-2, entering-distance, calibrated); (2) src/store/studyStore.ts — replaced isCalibrating boolean with calibrationStage state machine, added previousCalibration for cancel/restore, replaced setCalibrationPoint(index,point) with placeCalibrationPoint(point) (stage-driven), added confirmPoint1, confirmPoint2, resetPoint1, resetPoint2, confirmCalibration(knownDistanceMm), moveCalibrationPoint(which,point), cancelCalibration restores previous calibration, removed computeCalibration (logic moved into confirmCalibration), updated all initial state blocks (create, newStudy, loadStudy, clearCalibration) to reset calibrationStage to "idle" or "calibrated"; (3) src/components/ImageViewer.tsx — ROOT CAUSE FIX: calibration placement moved from handleOverlayClick (onClick) to handlePointerDown (pointerdown) as highest priority check, added calibration point drag support via calibration-marker class and data-calibration-point attribute, added drag-calibration interaction mode, handleOverlayClick now only handles landmark placement (returns early if in any calibration stage), updated cursor logic to use isCalibratingStage() helper, added distance label on calibration line when calibrated; (4) src/components/CalibrationPanel.tsx — complete UI redesign for state machine: idle shows "Calibration required" + [Calibrate image], placing-point-1 shows "Step 1 of 3" + [Cancel], reviewing-point-1 shows "Point 1 placed" + [Confirm Point 1] [Reset Point 1] [Cancel], placing-point-2 shows "Step 2 of 3" + [Cancel], reviewing-point-2 shows "Point 2 placed" + [Confirm Point 2] [Reset Point 2] [Cancel], entering-distance shows "Step 3 of 3" + input field + [Confirm calibration] [Cancel], calibrated shows "Calibrated: X.XXXX mm/pixel" + [Recalibrate] [Remove]; (5) src/store/studyStore.test.ts — rewrote all tests for new API: replaced isCalibrating checks with calibrationStage, replaced setCalibrationPoint with placeCalibrationPoint, added state machine transition tests, cancel/restore tests, moveCalibrationPoint tests. BUILD PASS, TESTS 240/240 PASS (12 new tests), TSC PASS.
2026-08-17: TestBot — Verified/added 9 required calibration state machine tests. 3 already covered by DevBot (Tests 2, 4, 7). Added 7 new tests (Tests 1, 3, 5, 6, 8×2, 9) covering: startCalibration stage+null points, confirmPoint1 standalone, confirmPoint2 standalone, confirmCalibration exact mmPerPixel=0.2 at 100px/20mm, pan conflict (placing-point-1 and placing-point-2), and landmark conflict (all 5 landmarks preserved). Total tests: 240→247. BUILD PASS, TSC PASS, TESTS PASS (247).

- 2026-08-17: QABot — Independent final verification of calibration state machine redesign

VERDICT: PASS

Checklist results (15/15 PASS):

1. Build/Tests/TSC: PASS
   - npm run build: PASS (exit 0, vite build successful, 243.85 kB bundle)
   - npm test: PASS (247/247 tests pass across 3 test files)
   - npx tsc --noEmit: PASS (exit 0, no type errors)

2. CalibrationStage type: PASS
   - src/domain/types.ts lines 51-58: CalibrationStage type with all 7 stages
   ("idle", "placing-point-1", "reviewing-point-1", "placing-point-2",
   "reviewing-point-2", "entering-distance", "calibrated")
   - Located in domain layer (types.ts), no React imports in src/domain/

3. State machine transitions (no skipping): PASS
   - studyStore.ts: placeCalibrationPoint guards on placing-point-1/placing-point-2 (L540-558)
   - confirmPoint1 guards on reviewing-point-1 (L579-583)
   - confirmPoint2 guards on reviewing-point-2 (L586-590)
   - resetPoint1 guards on reviewing-point-1 (L593-600)
   - resetPoint2 guards on reviewing-point-2 (L603-613)
   - confirmCalibration guards on entering-distance (L616-618)
   - cancelCalibration works from any stage (L518-538, no stage guard)
   - No stage can be skipped — every transition requires the correct prior stage

4. Click reliability fix (root cause): PASS
   - ImageViewer.tsx: calibration placement is in handlePointerDown (L220-226), NOT onClick
   - Calibration check is FIRST in handlePointerDown (highest priority)
   - Returns immediately after placing (L225: "return;") — no fallthrough
   - NO setPointerCapture called during calibration placement (verified by code scan)

5. Interaction priority: PASS
   - Priority order in handlePointerDown (L198-204 comment, L216-294 code):
     1. Calibration placement → 2. Calibration drag → 3. Landmark drag →
     4. Landmark placement → 5. Pan
   - Calibration click returns before reaching pan or landmark code
   - A calibration click can NEVER pan or place a landmark

6. Calibration point drag before confirmation: PASS
   - Calibration points draggable during reviewing-point-1/reviewing-point-2
   - Drag uses moveCalibrationPoint action (L302-304)
   - moveCalibrationPoint guarded to review stages only (studyStore L562-576)
   - After confirmation, stage leaves review, so drag is prevented

7. Calibration markers small: PASS
   - Visible calibration marker radius: r={0.005} (L656, L690)
   - Transparent hit area radius: r={0.014} (L648, L684)
   - Anatomical landmarks also: visible r={0.005} (L746), hit area r={0.014} (L739)

8. Calibration panel UI: PASS
   - CalibrationPanel.tsx: ALL 7 stages have correct messages and buttons:
     - idle: "Calibration required" + "Calibrate image" button (L74-88)
     - placing-point-1: Step 1 instructions + Cancel (L92-107)
     - reviewing-point-1: "Point 1 placed" + Confirm Point 1 + Reset Point 1 + Cancel (L110-139)
     - placing-point-2: Step 2 instructions + Cancel (L142-157)
     - reviewing-point-2: "Point 2 placed" + Confirm Point 2 + Reset Point 2 + Cancel (L160-189)
     - entering-distance: Step 3 instructions + distance input + Confirm calibration + Cancel (L192-232)
     - calibrated: mm/pixel display + Recalibrate + Remove (L235-262)

9. Cancel/Recalibrate safety: PASS
   - startCalibration saves previousCalibration (studyStore L501-516)
   - cancelCalibration restores previousCalibration (L518-538)
   - cancelCalibration does NOT change landmarks or measurements (test L238-250 verifies)
   - Recalibrate goes through startCalibration (CalibrationPanel L248-249, handleStart)

10. Reset behavior: PASS
    - resetPoint1 clears point1 and sets stage="placing-point-1" (L593-600)
    - resetPoint2 clears point2 (NOT point1) and sets stage="placing-point-2" (L603-613)
    - Tests verify both (L189-211)

11. Coordinate accuracy: PASS
    - Calibration points use screenToNormalized() — same function as landmarks (L221, L303)
    - No separate coordinate math for calibration

12. Calibration line and visual feedback: PASS
    - Calibration line drawn between both points when they exist (L608-634)
    - Distance label shows when calibrated (L620-632: realDistanceMm.toFixed(1) + " mm")
    - Point 1 stays visible while placing Point 2 (L640: rendered independently of stage)

13. Existing features preserved: PASS
    - 4 measurement lines: ramusR, ramusL, bodyR, bodyL (L412-445)
    - mm labels when calibrated, "calibration required" when not (L559-562)
    - Conclusion with actual mm values (ResultsPanel L341-349)
    - 2D projection warning (ResultsPanel L360-366)
    - Study persistence via localStorage (studyRepository)
    - Zoom/pan works when NOT calibrating (L280-294)
    - Landmark drag works when NOT calibrating (L246-261)
    - No "Mode A"/"Mode B" terminology in src/ (grep returned 0 matches)

14. Architecture: PASS
    - Domain layer (src/domain/) has zero React imports (grep confirmed)
    - No calculation logic in UI components (grep for domain functions in components: 0)
    - CalibrationStage type in types.ts (domain layer, L51-58)

15. Test coverage (9 required tests): PASS
    All 9 required spec tests found in studyStore.test.ts:
    - Test 1 (start calibration): "Test 1 — startCalibration sets stage and nulls both points" (L496)
    - Test 2 (place Point 1): "placeCalibrationPoint in placing-point-1 places only point1" (L139)
    - Test 3 (confirm Point 1): "Test 3 — confirmPoint1 transitions to placing-point-2" (L507)
    - Test 4 (place Point 2): "placeCalibrationPoint in placing-point-2 places only point2" (L150)
    - Test 5 (confirm Point 2): "Test 5 — confirmPoint2 transitions to entering-distance" (L521)
    - Test 6 (enter distance + confirm, mmPerPixel=0.2): "Test 6 — confirmCalibration with 100px distance" (L536)
    - Test 7 (no double placement): "placing point1 does NOT also set point2" (L320)
    - Test 8 (pan conflict): "Test 8 — calibration placement does not change pan or landmarks" (L557, L594)
    - Test 9 (landmark conflict): "Test 9 — calibration placement preserves all 5 landmark coordinates" (L623)
    Total: 247 tests (41 store + 169 domain + 37 coordinate), all passing

Issues found: None.
Notes: The calibration state machine redesign is clean, well-tested, and correctly
implements all required behaviors. The root cause fix (moving calibration placement
from onClick to pointerdown) is properly implemented with no setPointerCapture in the
placement path. All existing features are preserved.

- 2026-08-17: OrthoBot & UXBot — Created comprehensive clinician user manual `docs/user-manual-landmarks.md` covering sequential 5-point landmark placement (CoR, GoR, CoL, GoL, Me), calibration references, viewport navigation tools (pan/zoom/contrast), and clinical artifact avoidance. Updated `README.md`.
- 2026-08-17: PMBot — Reorganized multi-agent development team in `openclaw-ai-dev-team-maxroyak`. Migrated domain experts (`ortho_bot`, `vision_bot`, `research_bot`) into dedicated `domain_experts/` directory, added `ux_bot`, unified `dev_bot` and `qa_bot`. Cleaned up standalone agent files in `mandibular-asymmetry/agents/`.
- 2026-08-17: DevBot — Implemented full Internationalization (i18n) for English and Russian. Created typed localization subsystem in `src/locales/` (`types.ts`, `en.ts`, `ru.ts`, `index.ts`), added `LanguageSwitcher.tsx` header segmented toggle (`EN | RU`), updated domain layer conclusion builders in `src/domain/mandibularAsymmetry.ts` (`generateRamusComparison`, `generateBodyComparison`, `generateMandibularAsymmetryConclusion`, `generateClinicalSummary` accepting `locale`), updated `studyStore.ts` with `language` state & `localStorage` persistence, and localized all 7 UI components (`AnalysisPage`, `ResultsPanel`, `CalibrationPanel`, `ImageViewer`, `LandmarkPalette`, `StudyManager`, `ImageUploadZone`).
- 2026-08-17: TestBot & QABot — Authored Russian localization and language switching unit tests in `src/domain/mandibularAsymmetry.test.ts` and `src/store/studyStore.test.ts`. Verified 295/295 tests passing, TypeScript typecheck clean, ESLint clean, and Vite build successful.
- 2026-08-17: GitBot — Committed (`8bf6aea`) and pushed i18n support to remote `origin/master`.
- 2026-08-17: UXBot & DevBot — Implemented Track 1: Clinical PDF / Print Export. Built `src/components/ClinicalReportModal.tsx` displaying a formatted 1-page clinical standard report preview with clinic branding, patient ID, date, calibration scale, high-resolution radiograph with aligned SVG vector measurement overlay, quantitative metric table (Ramus & Body measurements with Habets Asymmetry Index), clinical conclusion, and mandatory 2D limitation disclaimer. Added `@media print` rules in `src/styles/index.css` configured for A4/Letter portrait output with automatic background UI suppression and client-side `window.print()` PDF generation. Added export buttons to `AnalysisPage.tsx` and `ResultsPanel.tsx`.
- 2026-08-17: TestBot & QABot — Created `src/test/reportExport.test.ts` verifying data payload integrity and i18n formatting across EN and RU. Verified 300/300 unit tests passing (100%), typecheck clean, ESLint clean, and Vite production bundle clean.
- 2026-08-17: DevBot, TestBot & QABot — Implemented Track 2: Native DICOM (.dcm) Radiograph Support & Auto-Scale. Integrated `dicom-parser` with typed definitions `src/types/dicom-parser.d.ts`, implemented `src/domain/dicom/dicomReader.ts` and `src/domain/dicom/types.ts` handling 8/12/16-bit monochromatic radiograph rendering, VOI windowing, photometric interpretations (MONOCHROME1 / MONOCHROME2), metadata extraction (Patient ID, Study Date), and auto-calibration via Pixel Spacing (`0028,0030`) and Imager Pixel Spacing (`0018,1164`). Extended `ImageUploadZone.tsx` to ingest `.dcm` files with automatic study creation and scale population. Authored test suite `src/test/dicomParser.test.ts`. 310/310 unit tests passing (100%), typecheck clean, ESLint clean, and Vite build successful.
- 2026-08-17: VisionBot, UXBot, DevBot & QABot — Implemented Track 3: AI-Assisted Landmark Detection via Manual Trigger Button (Phase 2). Built pure domain heuristic landmark localization engine in `src/domain/ai/landmarkDetector.ts` and `src/domain/ai/types.ts` generating normalized coordinates and confidence scores for CoR, GoR, CoL, GoL, and Me. Added manual trigger `[✨ Auto-Detect Landmarks (AI)]` in `LandmarkPalette.tsx` and `ImageViewer.tsx` toolbar with loading spinner. Added clinician-in-the-loop candidate states (`aiCandidateLandmarks`), dashed halo rings on canvas overlay, `[Accept All AI Proposals]` and `[Clear AI Proposals]` actions, and automatic conversion from candidate to verified upon manual adjustment. Authored `src/test/aiDetection.test.ts`. 318/318 unit tests passing (100%), typecheck clean, ESLint clean, and Vite build successful.
- 2026-08-17: VisionBot, DevBot & QABot — Implemented AI Landmark Detection Accuracy & Content-Aware ROI Letterbox Cropping. Implemented `detectRadiographRoi` filtering out black letterbox/pillarbox padding (luminance < 15) to normalize candidate coordinates relative to active radiograph content. Refined anatomical proportional zones: Condylar (Y ∈ [0.15, 0.28], CoR X ∈ [0.12, 0.22], CoL X ∈ [0.78, 0.88]), Gonial (Y ∈ [0.65, 0.78], GoR X ∈ [0.15, 0.25], GoL X ∈ [0.75, 0.85]), Menton (Y ∈ [0.85, 0.93], X ∈ [0.48, 0.52]). Added DICOM bypass and updated test suite `src/test/aiDetection.test.ts`. 320/320 unit tests passing (100%), typecheck clean, ESLint clean, and Vite build successful.
- 2026-08-17: UXBot, DevBot & QABot — UI Refactoring: Relocated "Save Study" and "New Study" Actions to Top Header. Moved `[💾 Save Study]` (with unsaved changes pulse indicator) and `[➕ New Study]` (with discard confirmation guard) into central toolbar position in `AnalysisPage.tsx` header alongside the title. Streamlined `StudyManager.tsx` sidebar with a clean study status badge and saved studies drawer. 320/320 unit tests passing (100%), typecheck clean, ESLint clean, and Vite build successful.
- 2026-08-17: UXBot, DevBot & QABot — Implemented Calibration Points Drag-to-Adjust Functionality. Enabled interactive dragging and repositioning of Calibration Points P1 and P2 during `reviewing-point-1`, `reviewing-point-2`, `entering-distance`, and `calibrated` stages with coordinate clamping to `[0.0, 1.0]` and real-time scale updating. Prioritized calibration marker hit-testing in `ImageViewer.tsx` with grab/grabbing cursor feedback. Authored test suite in `src/store/studyStore.test.ts`. 323/323 unit tests passing (100%), typecheck clean, ESLint clean, and Vite build successful.
- 2026-08-17: VisionBot, DevBot & QABot — Fixed AI Landmark Vertical Misalignment & Improved ROI Cropping. Upgraded `detectRadiographRoi` with adaptive luminance variance analysis to reliably isolate non-pitch-black letterboxes, added fallback central Y clamp `[0.08, 0.90]`, and strictly constrained candidate anatomical coordinates (`CoR`/`CoL` Y ∈ [0.18, 0.28], `GoR`/`GoL` Y ∈ [0.60, 0.72], `Me` Y ∈ [0.80, 0.88], hard-clamping `Me.y <= 0.88`). Authored unit tests in `src/test/aiDetection.test.ts`. 324/324 unit tests passing (100%), typecheck clean, ESLint clean, and Vite build successful.







- 2026-08-17: DevBot, UXBot & QABot — Reverted UI/UX Redesign to AntiGravity Light Baseline. Restored clean clinical light theme across AnalysisPage, ResultsPanel, LandmarkPalette, and ImageViewer while preserving all underlying functional capabilities (DICOM, ROI cropping, PDF export, landmark drag-to-adjust). 324/324 tests PASS.
- 2026-08-17: DevBot, UXBot & QABot — Fixed ClinicalReportModal PDF Print Render Disconnect & Point Offset. Formatted report container with synchronized aspect-ratio wrapper and fixed print stylesheets for exact 1-page clinical print/PDF output.
- 2026-08-17: DevBot & QABot — Single Source of Truth for Overlay (WYSIWYG). Extracted unified `src/components/RadiographOverlay.tsx` shared between `ImageViewer.tsx` and `ClinicalReportModal.tsx` handling landmarks, AI proposals, calibration lines/markers, and measurement lines identically.
- 2026-08-17: DevBot, TestBot & QABot — Enforced Architectural Bounds & Visual E2E Quality Gate. Built unified `<RadiographCanvasContainer />` wrapper enforcing zero padding, zero margins, and matching natural aspect ratio across all views. Created automated visual parity test suite `src/test/visualParityE2E.test.tsx` verifying < 0.0001 (sub-pixel) delta between workspace and report preview. 330/330 tests PASS.
- 2026-08-17: DevBot & QABot — Fixed Oval Marker Distortion (Anisotropic Scale). Replaced normalized `viewBox="0 0 1 1"` with native pixel `viewBox={`0 0 ${natW} ${natH}`}`, ensuring 1:1 isotropic scaling and true round circles across all views. 330/330 tests PASS.
- 2026-08-17: DevBot, UXBot & QABot — Precision Clinical Landmark Markers. Reduced visible marker radii to 3.5px-4px with 1px center focal pinpoint dots for pinpoint anatomical placement over cortical bone, while maintaining 14px-16px transparent hit targets for effortless dragging. 330/330 tests PASS.
- 2026-08-17: DevBot, UXBot, TestBot & QABot — Added "Clear All" (`🗑️ Удалить всё` / `Clear All`) Bulk Deletion. Added `clearAllStudies()` in `studyStore.ts`, added header button in `StudyManager.tsx` with safety confirmation modal, added full EN/RU localization, and authored unit tests in `src/test/studyManager.test.tsx` and `src/test/integration.test.ts`. 336/336 tests PASS (10/10 test files). Production build verified clean.
- 2026-08-18: DevBot, UXBot, TestBot & QABot — Phase 2 Finalization: DICOM Auto-Scale, Precision Micro-Markers, Bulk Clear & CBCT Hint. (1) DICOM Auto-Calibration: Automatically extracted PixelSpacing (0028,0030) and ImagerPixelSpacing (0018,1164) upon DICOM load, initialized mmPerPixel, set source to "dicom", and displayed localized status badge ("✓ Автокалибровка по DICOM (0.XXXX мм/пкс)" / "✓ Auto-calibrated via DICOM (0.XXXX mm/px)") in CalibrationPanel and ResultsPanel while preserving 7-stage manual calibration fallback for raster formats. (2) Precision Micro-Markers & Hair-Crosses: Refined visible dot radii (3px-4px) and added subtle 1px center hair-crosses / focal points inside all anatomical landmarks (CoR, GoR, CoL, GoL, Me) and calibration handles (P1, P2) in RadiographOverlay.tsx for pinpoint joint placement over cortical bone, maintaining large 14px-16px touch targets. (3) Bulk Studies Deletion: Verified "Clear All" ("🗑️ Удалить всё") in StudyManager.tsx with safety confirmation dialog and complete i18n support in EN & RU. (4) CBCT 2D Export Hint: Added subtle info link ("Как загрузить срез из 3D КТ (КЛКТ)?") and 2-step instructional modal in ImageUploadZone.tsx explaining how to export 2D panoramas (5-15 MB) from CBCT viewers (Ez3D-i, Romexis, Planmeca, Carestream) instead of uploading 1GB 3D volumes. Created comprehensive test suite in src/test/phase2Enhancements.test.tsx. All 342/342 unit tests PASS (11/11 test files), TypeScript typecheck clean, and production build PASS.
- 2026-08-18: UXBot, DevBot, TestBot & QABot — Hidden Cursor During Active Marker Dragging for Precision View. Set `cursor: none` on `<RadiographOverlay />` SVG, interactive marker hit-targets, and `<ImageViewer />` frame container during active dragging (`isDraggingMarker === true`), completely removing the default OS grabbing hand icon to ensure 100% unobstructed visibility of underlying cortical bone contours and joint structures. Retained `cursor: grab` on hover. Authored unit test in `src/test/phase2Enhancements.test.tsx`. 343/343 tests PASS, TypeScript typecheck clean, Vite build PASS.

